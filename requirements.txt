# HALO runtime dependencies (host side).
# Sandbox-only packages (pwntools, scapy) are installed inside the container
# image — see sandbox/Dockerfile — and are intentionally not listed here.
requests>=2.28
Flask>=2.2
PyYAML>=6.0
